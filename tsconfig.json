{
    "include": ["src"], // include: js로 컴파일하고 싶은 디렉터리를 넣는다.
    "compilerOptions": {
        "outDir": "build", // outDir: js파일이 생성될 디렉터리를 지정한다.
        "esModuleInterop": true,
        "target": "ES6", //target: 어떤버전의 자바스크립트를 컴파일할지를 설정
        "lib": ["ES6"], // lib: 합쳐진 라이브러리의 Declaration file을 특정해주는 역할을 한다. 자동완성의 기능을 가능하게 해줌.
        "strict": true, // 타입의 엄격하게 관리한다.
        "module": "CommonJS"
    }
}